services:
  app:
    build: .
    ports:
      - "8081:8080"
    environment:
      - MONGO_URL=mongodb://mongo:27017/dd_db
    depends_on:
      - mongo

  mongo:
    image: mongo
    ports:
      - "27017:27017"
